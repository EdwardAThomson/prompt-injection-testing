# Shared LLM backend layer (llm-backends): ai_helper.py and cli_backends.py
# are facades over this package (StoryDaemon docs/LLM_BACKENDS_INVENTORY.md,
# section 7.4, step 5). Pinned git-URL install (the package is public); for
# day-to-day development against a sibling checkout use instead:
#     pip install -e ../llm-backends
llm-backends @ git+https://github.com/EdwardAThomson/llm-backends@v0.1.1

openai>=1.0.0
google-generativeai>=0.3.0
anthropic>=0.7.0
python-dotenv>=0.19.0
pyyaml>=6.0

# Tests (fakes only, no LLM calls): python -m pytest -q
pytest>=8.0

# Optional: BERT detector (only needed if bert detector is enabled)
# pip install torch transformers sentence-transformers
# torch>=2.0.0
# transformers>=4.35.0
# sentence-transformers>=2.2.0

# Optional: Agent Dojo integration (only needed for scramblegate runner)
# agentdojo>=0.1.34
